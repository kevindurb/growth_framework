const mobx = window.mobx;
const mobxPreact = window.mobxPreact;

export const decorate = mobx.decorate;
export const observable = mobx.observable;
export const action = mobx.action;
export const computed = mobx.computed;
export const spy = mobx.spy;

export const inject = mobxPreact.inject;
export const observer = mobxPreact.observer;
export const Provider = mobxPreact.Provider;
